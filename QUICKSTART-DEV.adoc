// SPDX-License-Identifier: CC-BY-SA-4.0
// QUICKSTART-DEV.adoc — clone → build → test → PR
= nexia-list — Quick Start for Developers
:toc:
:toclevels: 2

== Tech Stack

* **Bun 1.3+** — the only JS toolchain (packages, tasks, tests, bundler, dev server)
* **ReScript 11** — TEA-style UI on `@rescript/react`, bundled with Bun
* **Rust stable** — core engine (notes, backlinks, search, JSON storage),
  compiled to WASM via wasm-bindgen for the browser
* Optional: **Gossamer** desktop shell (external sibling checkout of
  `hyperpolymath/gossamer`; not built in this repo's CI)

== Set Up Development Environment

=== Option A: Guix (preferred)

[source,bash]
----
guix shell
----

=== Option B: Manual

[source,bash]
----
git clone https://github.com/hyperpolymath/nexia-list.git
cd nexia-list
just setup    # bun install + rustup wasm32 target
----

== Build

[source,bash]
----
bun run build       # ReScript compile + Bun web bundle
bun run build:wasm  # Rust core → WASM for the browser
----

== Test

[source,bash]
----
bun run test        # Rust core (cargo test) + UI (bun test)
----

== Run

[source,bash]
----
bun run dev         # dev server on http://localhost:5173
----

== Project Structure

[source]
----
nexia-list/
├── core/                 # Rust core engine + λδ substrate (91 passing tests)
├── ui/                   # ReScript TEA-style UI
│   └── src/bindings/     # DOM bindings
├── scripts/              # Bun build/dev scripts
├── web/                  # Browser entry + bundle output (dist/)
├── desktop/              # OPTIONAL Gossamer shell (external dependency)
├── docs/adr/             # Architecture decision records
├── tests/                # Cross-cutting tests
├── .machine_readable/    # Checkpoint files (STATE, META, ECOSYSTEM) + contractiles
├── Justfile              # Task runner recipes (delegate to Bun tasks)
├── package.json          # Bun tasks and dependencies
├── bun.lock              # Exact JavaScript dependency graph
├── guix.scm              # Guix environment
└── 0-AI-MANIFEST.a2ml    # AI agent entry point
----

== Key Recipes

[source,bash]
----
just setup          # Install dependencies (+ wasm target)
just build          # Build the project
just build-wasm     # Build the WASM core
just test           # Run tests
just run            # Start the dev server
just check          # Biome lint + rustfmt --check + clippy
just doctor         # Self-diagnostic
just assail         # Security scan via panic-attacker (if installed)
just tour           # Guided tour of the codebase
----

== Before Submitting a PR

[source,bash]
----
just check          # Lint and static checks
just test           # All tests pass
bun run fmt         # Format (Biome + cargo fmt)
----

== Contractile Invariants

Read `.machine_readable/MUST.contractile` before making changes.
Key invariants that must never be violated:

* **Bun only** — no npm/deno/yarn/pnpm CLIs or lockfiles
* **No new TypeScript, Python, or Go files**
* **SPDX-License-Identifier header on every source file** (MPL-2.0 for code,
  CC-BY-SA-4.0 for docs); never remove or modify LICENSE
* **No hardcoded absolute paths** (`/home/*`, `/mnt/*`, `/var/mnt/*`) — use
  env vars, XDG dirs, or relative references
* **No `unsafe {}` Rust blocks without a safety comment**
* **`.machine_readable/` and `0-AI-MANIFEST.a2ml` preserved**; no SCM files in
  repo root
* **No removal of CI workflows without explicit approval**; all GitHub Actions
  SHA-pinned
* **Tests must not be deleted or weakened**

== LLM/AI Agent Development

If using an AI assistant, load the warmup context first:

[source,bash]
----
just llm-context    # Outputs role-appropriate context
----

Or read `0-AI-MANIFEST.a2ml` directly.

== Get Help

* **Architecture**: link:EXPLAINME.adoc[EXPLAINME.adoc] and link:docs/adr/[docs/adr/]
* **Wiki**: https://github.com/hyperpolymath/nexia-list/wiki
* **Report issue**: `just help-me`
